{
  "name": "desafio5-piedrapapeltijera",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /desafio5-piedrapapeltijera",
    "deploy": "gh-pages -d build",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.21",
    "gh-pages": "^6.1.1",
    "parcel": "^2.11.0"
  }
}